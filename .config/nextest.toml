[profile.default]
retries = 2
failure-output = "immediate-final"
fail-fast = { max-fail = 10 }
leak-timeout = "1s"

[profile.ci]
# Do not cancel the test run on the first failure
fail-fast = false
retries = 2
failure-output = "immediate-final"
leak-timeout = "1s"

# Temporary: validates the sccache build cache. Windows-only while fixing its
# server IPC under concurrency; restore linux+macos jobs for the final run.
# Remove before merging.
name: 🧪 Test build cache

on:
  push:
    branches:
      - eric/cache-ci-builds

permissions:
  id-token: write
  contents: read

jobs:
  windows_x86_64:
    uses: ./.github/workflows/release_windows.yml
    with:
      ARCHITECTURE: x86_64
      AWS_ROLE: arn:aws:iam::213020545630:role/github-runner-role
      AWS_REGION: us-west-1
      PYTHON_VERSIONS: "3.11"
    secrets:
      GH_PERSONAL_ACCESS_TOKEN: ${{ secrets.GH_PERSONAL_ACCESS_TOKEN }}

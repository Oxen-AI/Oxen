# Deprecations and scheduled removals

Central registry of deprecated behavior and the release each item should be **removed** in. Code-level `// DEPRECATED` / `// TODO: remove` comments are easy to lose track of, so anything with a concrete removal target is recorded here.

**Workflow:** when you deprecate something in code with a removal target, add a row here; when you remove it, delete the row. A scheduled weekday check (see "Reminder" below) reads the **Remove in** column, compares it against the latest released version, and alerts before a removal release so the item can be dropped in a PR first.

## Pending removals

| Item | Deprecated in | Remove in | Location | Notes |
| --- | --- | --- | --- | --- |
| Filesystem merkle node backend — `MerkleNodeBackend::Filesystem` / `FsMerkleNodeStore`, the `merkle_node_backend` config field, the client's backend selector, and the LMDB→filesystem `down` migration. The CLI refuses guarded commands on a filesystem-backed repo from the deprecation release on, pointing at `oxen migrate up merkle_nodes_to_lmdb`. | 0.54.0 | ~0.59.0 | `crates/liboxen/src/core/db/merkle_node/` (`FsMerkleNodeStore`, `MerkleNodeBackend`); gate in `crates/liboxen/src/command/migrate/m20260626_migrate_merkle_nodes_to_lmdb.rs` (`is_needed`); selector in `crates/oxen-cli/src/cmd/clone.rs`, `init.rs`, and `create_remote.rs`, rejected via `reject_deprecated_merkle_backend` in `crates/oxen-cli/src/helpers.rs` | Needs a final fleet sweep before removal — any repo still on the filesystem backend becomes unloadable, which is why this carries the full five-minor horizon rather than a single release. The `down` direction goes with it, so the transcode becomes one-way. |
| `commits` no-op echo endpoint — `POST /repos/{ns}/{name}/commits/mark_commits_as_synced`, which accepts hashes and echoes them back without writing anything. | ≤ 0.50.4 (already shipped) | ~0.59.0 | `crates/oxen-server/src/controllers/commits.rs` (`mark_commits_as_synced`); route in `crates/oxen-server/src/services/commits.rs`; OpenAPI entry in `crates/oxen-server/src/main.rs` | The Oxen client stopped calling this in 0.54.0. The endpoint has to outlive every client that still sends the POST: those clients propagate the failure, so a 404 fails their push outright even though the call does nothing. Remove once 0.54.0 is the oldest client version in the field. Chunk 1 of ENG-994. |
| Server config `[identity] repo_uuids_assigned_by` — selects whether a request's namespace and name positions carry human-readable names or UUIDs assigned above the server | 0.54.0 (added transitional) | When ENG-1609 lands | `crates/oxen-server/src/config/identity_policy.rs` (both `IdentityPolicy` and the `IdentitySource` enum it parses); `Config::identity` in `crates/oxen-server/src/config.rs` | Transitional from the day it shipped. It selects two behaviors that still differ: whether a create must carry a `repo_uuid` (required under `auth-provider`, assigned by the server under `oxen-server`), and where a repository's display names come from (the request positions under `oxen-server`, the request body under `auth-provider`). Both forks close once the control plane stops supplying the UUID (ENG-1608) and puts real names in the positions (ENG-1609), which is also when `RepoNew`'s `repo_uuid` goes. Condition-gated rather than release-gated, since those are what actually settle it. Deliberately absent from `SelfHosting.md`: the default needs no admin action, so only a deployment behind a control plane ever sets it. |
| Workspace config compatibility with the pre-rename filename — reading `WORKSPACE_CONFIG` alongside `workspace.toml` — together with the `backfill_workspace_created_at` migration that records an approximate creation time for workspaces lacking one | 0.54.0 (added transitional) | 5 minors after the workspace reaper ships | `WORKSPACE_CONFIG_LEGACY` in `crates/liboxen/src/constants.rs`; `Workspace::legacy_config_path_from_dir` and `existing_config_path_from_dir` in `crates/liboxen/src/model/workspace.rs`; `crates/liboxen/src/command/migrate/m20260824_backfill_workspace_created_at.rs` and its `ALL_MIGRATIONS` entry | Both serve the same population: no recorded creation time means the workspace predates the release that started recording one, and a config under the former filename means it predates the rename. The reaper skips workspaces with no creation time for its first five minor releases, then treats them as reapable — after which neither the fallback nor the migration has anything left to serve. Condition-gated on the reaper rather than release-gated, since that is what starts the clock. |

## Removed without a deprecation window

Removals that intentionally skipped the additive-deprecation workflow (owner decision), recorded here so client breakage reports can be traced to a release.

| Item | Removed in | Location (was) | Notes |
| --- | --- | --- | --- |
| JSON workspace-staging endpoint — `POST /repos/{ns}/{name}/workspaces/{id}/versions/{dir}`, where the client pre-hashed content and the server staged metadata without reading it. Clients use the multipart `POST /workspaces/{id}/files/{path}` endpoint instead. | 0.54.0 | `crates/oxen-server/src/controllers/workspaces/files.rs` (`add_version_files`); gate in `crates/oxen-server/src/params.rs` (`client_must_use_multipart_staging`); `crates/liboxen/src/core/v_latest/workspaces/files.rs` (`add_version_files`); Hub proxy route in OxenHub `lib/oxen_api_web/routers/oxen_server_router.ex` | Deprecated in 0.51.0 with a 426 for Oxen clients at or above that release, so those had a full window. The gate deliberately let requests with no User-Agent, a non-Oxen agent, or an unparseable version through, so browsers and third-party integrations never received the 426 and see this removal as a 404 with no prior warning. Recorded here for that population. `OxenHttpError::EndpointDeprecated` went with it, having had no other construction site. |
| Workspace data frame staged diff — `GET /repos/{namespace}/{name}/workspaces/{workspace_id}/data_frames/diff/{path}` and the `oxen workspace diff` CLI command | 0.52.0 (workspace reversibility removal) | `crates/oxen-server/src/services/workspaces/data_frames.rs`; `crates/oxen-cli/src/cmd/workspace/diff.rs` | Older CLIs receive a 404. Removed with the change-tracking ("mods") layer; there is no replacement — edits apply directly. |
| Workspace row/column restore — `POST /repos/{namespace}/{name}/workspaces/{workspace_id}/data_frames/rows/{row_id}/restore/{path}` and `POST /repos/{namespace}/{name}/workspaces/{workspace_id}/data_frames/columns/{column_name}/restore/{path}` | 0.52.0 (workspace reversibility removal) | `crates/oxen-server/src/controllers/workspaces/data_frames/rows.rs`, `.../columns.rs` | Older clients receive a 404. Whole-data-frame restore (`DELETE .../data_frames/resource/{path}`, re-index) remains. |
| Pre-0.25.0 repo on-disk formats — loading a repo whose `.oxen/config.toml` `min_version` is `0.10.0` or `0.19.0`. The `v_old` merkle read path was deleted, so these formats can no longer be read. | 0.51.4 (`v0.19.0` / `v_old` read path dropped in #730; `0.10.0` was never supported in this lineage) | `crates/liboxen/src/core/v_old/` (deleted in #730); rejection guard in `crates/liboxen/src/model/repository/local_repository.rs` (`new_with_server_opts`, version literals inlined) | Loading such a repo errors at construction with `UnsupportedRepoVersion`; the hint points to migrating up with an older Oxen release first. Over HTTP every endpoint for the repo returns 400 with `type: "unsupported_repo_version"` — terminal, so clients don't retry it. |

## Reminder

There is no automated scheduler for this list (a Claude session cron expires after 7 days — too short for these multi-release horizons — and a persistent CI job wasn't wanted yet). Instead, the workspace `CLAUDE.md` ("Wrapping up work") tells the agent to **offer**, when work wraps up, a quick scan that cross-checks the **Remove in** targets above against the current/upcoming release and flags anything due for removal. Keep this list current so that scan stays useful.
